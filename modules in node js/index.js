const math = require("./math");// for requiring custom js library (in our case its in current folder);
const {sub, add, multi} = require("./math"); // we can destructure our required functions


console.log(multi(5, 10));



